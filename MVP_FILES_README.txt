These files are meant to be copied into your existing Azure Static Web Apps (Next.js hybrid) repo.

Default target structure (common):
  src/app/...
  src/components/...

If your repo uses 'app/' at root (no src/), move:
  src/app/*  -> app/*
  src/components/* -> components/*
